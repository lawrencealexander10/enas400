<div class="container">
	<span> Shopping Cart</span>
	<%= render 'items', :order => current_order %>
	<div class="group-buttons">
		<%= button_to 'Empty basket', basket_path, :method => :delete %>
		<%= button_to( 'Continue Shopping', root_path)  %> 
		<%= button_to "Checkout", checkout_path, :method => :get, :style => "float: right"  %>
	</div>
</div>